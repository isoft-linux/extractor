0.92版本：
1，修复部分视频格式按照时间间隔抓帧不全的问题。可能是packet.dts跳跃导致的，现在的解决办法是找到第一帧之后，就一帧一帧的读取，知道达到时间间隔为止。

0.91版本：
1，重构，重命名了部分函数。

0.9版本：
1，使用native代码抓帧，不再调用ffmpeg.exe，提升抓帧速度N倍。

0.8版本：
1，修复了framedialog没有载入文件敲回车的崩溃问题。

0.7版本：
1，取到flv正确的帧数。

0.6版本：
1，分帧抓取界面加入进度指示器。
2，加入按钮图标，调整部分UI。
3，使用ts做翻译。

0.5版本：
验证用

0.4版本：
1，预览窗口的图片缩小为256宽度，也就是抓出来就是小图，这样显示较多图片程序不至于失去响应。
2，默认时间间隔改为4秒。
3，保存文件的目录名修改为"集名-selected"
4，保存的文件名修改为"集名-000数字".
5，加了个应用程序图标。

0.3版本：
1，临时修复flv取不到帧数的问题，用20帧代替。

0.2版本：
1，加入预览列表。
2，加入分帧抓取列表和界面，并处理快捷键。

0.1版本：
1，实现抓帧功能
2，用资源管理器打开目录列表。
